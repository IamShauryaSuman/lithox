[package]
name = "lithox"
version = "0.1.1"
edition = "2024"
description = "A persistent, on-disk B+ Tree storage engine and buffer pool manager."
license = "MIT OR Apache-2.0"
repository = "https://github.com/IamShauryaSuman/lithox"

[dependencies]
bytemuck = { version = "1.25.2", features = ["derive"] }
